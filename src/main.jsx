import React from "react";
import ReactDOM from "react-dom/client";
import WorkflowMapper from "./WorkflowMapper.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WorkflowMapper />
  </React.StrictMode>
);
